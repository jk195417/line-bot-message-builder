RSpec.describe Line::Bot::MessageBuilder do
  it "has a version number" do
    expect(Line::Bot::MessageBuilder::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
